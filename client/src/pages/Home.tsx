import { Layout, DashboardStats, SocialMediaLinks } from '../components';

export const Home = () => {
  return (
    <Layout>
      <DashboardStats />
      <SocialMediaLinks />
    </Layout>
  );
};
